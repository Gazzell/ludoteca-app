import { createTheme } from "@mui/material";
const theme = createTheme();

export const mainContainer = {
  display: "flex",
  alignItems: "center",
};
export const smMargin = {
  margin: theme.spacing(1),
};
export const actionDiv = {
  textAlign: "center",
};
